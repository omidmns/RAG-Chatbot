version: '3'
services:
  api:
    build: ./api
    volumes:
      - ./api:/app
    env_file:
      - .env
    ports:
      - "5000:5000"
    networks:
      - api_network
    privileged: true
    runtime: nvidia
networks:
  api_network:
    driver: bridge
